query {
    customers (order_by: {id: desc}) {
        id
        first_name
        last_name
        email
    },
    addresses (order_by: {street: desc}) {
        id
        street
        house_number
    }
}