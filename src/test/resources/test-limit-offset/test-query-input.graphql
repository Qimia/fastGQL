query {
    customers (limit: 1, offset: 0) {
        id
        first_name
        last_name
        email
    }
    addresses (limit: 1, offset: 1) {
        id
        street
        house_number
    }
}