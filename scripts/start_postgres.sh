docker run --ulimit memlock=-1:-1 -it --rm=true --memory-swappiness=0 --name postgres_test -e POSTGRES_USER=postgres_test -e POSTGRES_PASSWORD=postgres_test -e POSTGRES_DB=postgres_test -p 5432:5432 -v $PWD/initdb/postgres:/docker-entrypoint-initdb.d debezium/postgres:11
